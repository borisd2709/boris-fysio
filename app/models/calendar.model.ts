export class Calendar {
    name: string;
    id: string;
    src: string;

    constructor(name: string, id: string, src: string) {
        this.name = name;
        this.id = id;
        this.src = src;
    }
}